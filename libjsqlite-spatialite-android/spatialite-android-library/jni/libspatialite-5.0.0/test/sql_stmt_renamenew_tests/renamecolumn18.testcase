RenameColumn - TEXT, TEXT, TEXT permissive NULL
:memory: #use in-memory database
SELECT RenameColumn('main', 'table', 'old_column', 'new_column', NULL);
1 # rows (not including the header row)
1 # columns
RenameColumn('main', 'table', 'old_column', 'new_column', NULL);
RenameColumn exception - invalid argument (5th arg).
