RenameColumn - TEXT, TEXT, TEXT
:memory: #use in-memory database
SELECT RenameColumn('main', 'table', 'old_column', 'new_column');
1 # rows (not including the header row)
1 # columns
RenameColumn('main', 'table', 'old_column', 'new_column');
RenameColumn exception - not existing table [main.table].
