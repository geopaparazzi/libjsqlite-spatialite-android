SqlProc_ExecuteLoop - INT
:memory: #use in-memory database
SELECT SqlProc_ExecuteLoop(1);
1 # rows (not including the header row)
1 # columns
SqlProc_ExecuteLoop(1)
SqlProc exception - illegal SQL Procedure arg [not a BLOB].
