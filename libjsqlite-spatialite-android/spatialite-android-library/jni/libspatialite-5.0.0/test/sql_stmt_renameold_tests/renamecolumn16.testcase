RenameColumn - BLOB DB-prefix
:memory: #use in-memory database
SELECT RenameColumn(zeroblob(10), 'table', 'old_column', 'new_column');
1 # rows (not including the header row)
1 # columns
RenameColumn(zeroblob(10), 'table', 'old_column', 'new_column');
RenameColumn exception - libsqlite 3.25 or later is strictly required.
