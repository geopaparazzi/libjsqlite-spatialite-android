RenameTable - TEXT, TEXT permissive NULL
:memory: #use in-memory database
SELECT RenameTable(NULL, 'old_table', 'new_table', NULL);
1 # rows (not including the header row)
1 # columns
RenameTable(NULL, 'old_table', 'new_table', NULL);
RenameTable exception - libsqlite 3.25 or later is strictly required.
