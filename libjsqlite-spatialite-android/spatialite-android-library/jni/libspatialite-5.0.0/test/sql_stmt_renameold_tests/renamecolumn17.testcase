RenameColumn - TEXT, TEXT, TEXT permissive
:memory: #use in-memory database
SELECT RenameColumn('main', 'table', 'old_column', 'new_column', 1);
1 # rows (not including the header row)
1 # columns
RenameColumn('main', 'table', 'old_column', 'new_column', 1);
RenameColumn exception - libsqlite 3.25 or later is strictly required.
